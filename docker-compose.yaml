version: '3'

services:
  db:
    image: postgres:16.2-bookworm
    restart: always
    environment:
      POSTGRES_USER: developer
      POSTGRES_PASSWORD: developer
      POSTGRES_DB: dev
    ports:
      - "5432:5432"